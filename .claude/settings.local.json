{
  "permissions": {
    "allow": [
      "Bash(dotnet --version)",
      "Bash(dotnet --list-sdks)",
      "Bash(docker --version)",
      "Bash(docker compose *)",
      "Bash(dotnet sln *)"
    ]
  }
}
